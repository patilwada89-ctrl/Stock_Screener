Read `/Users/ashish/vscode/Stock_Screener/AGENTS.md` first, then `/Users/ashish/vscode/Stock_Screener/docs/context.md`.

Use `app.py` for Streamlit UI only and keep all domain logic in `src/` as pure functions. If you change behavior/contracts, update docs in-repo (`README.md`, `docs/*`, or `AGENTS.md`) in the same PR.
